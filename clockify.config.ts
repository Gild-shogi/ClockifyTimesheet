import { AppConfig } from './src/types';

/**
 * Clockify勤務表生成ツールの設定ファイル
 *
 * このファイルをカスタマイズして、勤務表の見た目やタイムゾーンを変更できます。
 * APIキーなどの機密情報は .env ファイルで設定してください。
 */
const config: AppConfig = {
  // ========================================
  // タイムゾーン設定
  // ========================================
  // 勤務時間の計算に使用するタイムゾーン
  // 例: 'Asia/Tokyo', 'America/New_York', 'Europe/London'
  timezone: 'Asia/Tokyo',

  // ========================================
  // Excel出力設定
  // ========================================
  excel: {
    // ----------------------------------------
    // 色設定（16進数、#なし）
    // ----------------------------------------
    // ヘッダー行の背景色
    headerColor: '4472C4',

    // 交互行の背景色（見やすさのため1行おきに色をつける）
    alternateRowColor: 'F2F2F2',

    // ----------------------------------------
    // 罫線設定
    // ----------------------------------------
    // セルの罫線スタイル: 'thin' | 'medium' | 'thick'
    borderStyle: 'thin',

    // 日付グループの区切り線（日付が変わる行の上に引く太い線）
    dateGroupBorder: {
      style: 'medium',
      color: '333333',
    },

    // ----------------------------------------
    // フォント設定
    // ----------------------------------------
    // フォントサイズ（ポイント）
    fontSize: 11,

    // フォント名
    // Windows: 'Meiryo UI', 'Yu Gothic UI', 'MS Gothic'
    // macOS: 'Hiragino Sans', 'Hiragino Kaku Gothic ProN'
    fontName: 'Meiryo UI',
  },
};

export default config;
